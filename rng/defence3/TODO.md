for some reason total is well over 200k even though I can see that units stop because the budget goes over, but that is not reflected in the numbers. Also, for some reason healer seems to use like 1000 times more budget than other units. Also, cannot see the question mark glyphs currently (when run out of A*) or red energy glyphs when run out of energy.

Add a new resource: A*. A* is similar to energy, but instead of being yellow, it is red. A* harversing works a bit differently from harvesting enery. Instead of having A* units that harverst it. There is only a A* building, that is built next to a A* resources tile. The A* building harvests from 1 A* resources at a time (above, below, left, right). each A* adds to the available A* budget a variable amount, such that A* budget <= max A* budget that was configured in main menu. By default that is by 500, but can be upgraded in the researcher (sof cap of 3000). Each A* resource is limited, add similar fields in the main menu resources and research as is for gold mines (as these are effectively A* mines). Essencially A* is fairly similar to energy, except that it does not need A* units. Each A* building addds to the available A*, and each unit uses that. optimize the A* collection. Also, implement workers a max distance to the target, i.e, such that they only search for new work within a radius. Add this as stat for the workers, and set the default to 10, with a max softcap of 25, upgradeable in the research building. Also, the top bar should now show available A* / max A*.